% Imports
\usepackage{tocloft}            % Untuk daftar isi/referensi/gambar/tabel
\usepackage{tabularx}           % Untuk tabel dengan lebar otomatis
\usepackage{longtable}          % Untuk tabel yang bisa memanjang ke halaman berikutnya
\usepackage{graphicx}           % Untuk gambar
\usepackage{array}              % Untuk mengatur format tabel
\usepackage{ragged2e}           % Untuk rata kiri pada tabel
\usepackage{booktabs}           % Untuk centering
\usepackage{tabto}              % Untuk mengatur indentasi
\usepackage{enumitem}           % Untuk numbering
\usepackage{listings}           % Untuk memasukkan potongan kode program
\usepackage{xcolor}             % Untuk warna pada kode listing
\usepackage{pdfpages}           % Untuk include pdf
\usepackage{pgffor}             % Untuk looping
\usepackage[indonesian]{babel}
\usepackage[export]{adjustbox}  % Styling includegraphics (show border)
\usepackage{lipsum}             % Generate dummy text - is not mandatory

%=====================================
% Metadata
\input{template/metadata.tex}

%=====================================
% Font Formatting 
\usepackage{fontspec}
\setmainfont{Calibri}
\setmonofont{Consolas}[LetterSpace=-5.0]
\usepackage{microtype}         % allows LaTeX to "squeeze" or "stretch" letters slightly to fit the line
%=====================================
% Page Layout Formatting
\usepackage[
    a4paper,
    left=4cm,
    right=3cm,
    top=4cm,
    bottom=3cm,
    % showframe % enable to show margin frames
]{geometry}


\usepackage{fancyhdr} % Untuk header dan footer
\pagestyle{fancy}
\renewcommand{\headrulewidth}{0pt} % Removes the header line
\renewcommand{\footrulewidth}{0pt} % Removes the footer line
\fancyhf{} % Clear all headers and footers
\fancyfoot[R]{\thepage} % Set number to the Right

\fancypagestyle{plain}{
    \renewcommand{\headrulewidth}{0pt} % Removes the header line
    \renewcommand{\footrulewidth}{0pt} % Removes the footer line
    \fancyhf{} % Clear all headers and footers
    \fancyfoot[R]{\thepage} % Set number to the Right
}

%=====================================
% Default spacing ke 1.5 dan first line indent ke 1.27
\usepackage{setspace}
\onehalfspacing
\setlength{\parindent}{1.27cm}
\usepackage{indentfirst}

%=====================================
% Heading 1 (Bab) Style Formatting
\usepackage{titlesec}
\titleformat{\chapter}[display]
{\normalfont\bfseries\fontsize{14pt}{16pt}\centering} % Format Heading
{BAB \thechapter} % Label Heading
{0pt} % Spasi antara label dan judul
{\vspace{0.5ex}} % Agar judul di bawah nomor Bab

\titlespacing*{\chapter}
{0pt}   % indent kiri
{-20pt} % jarak dari atas halaman
{20pt}  % jarak setelah judul

% Heading 2 (Subbab) Style Formatting
\titleformat{\section}
{\normalfont\bfseries\fontsize{12pt}{14pt}\selectfont}
{\thesection}
{10pt}
{}

%=====================================
% Miscellanous Settings

% Disable break word pakai "-"
\hyphenpenalty=5000
\exhyphenpenalty=10000
\tolerance=9999
\emergencystretch=10pt

% Hyperlink styling
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=black,
    urlcolor=blue,
    citecolor=black
}

% ToC styling
\setlength{\cftbeforechapskip}{2pt}
\renewcommand{\cftchappresnum}{BAB }            % Set agar chapter punya awalan "BAB"
\renewcommand{\cftchapnumwidth}{3em}            % Tambah indentation agar tidak tabrakan
\renewcommand{\cftsecindent}{3em}            % Tambah indentation agar tidak tabrakan
\renewcommand{\cftchapfont}{\normalfont}        % Set agar chapter di daftar isi tidak bold
\renewcommand{\cftchappagefont}{\normalfont}    % Set agar numbering chapter tidak bold
\renewcommand{\cftdotsep}{0.75}                 % Set agar titik-titik tidak terlalu renggang
\renewcommand{\cftchapdotsep}{\cftdotsep}       % Set agar chapter juga punya titik-titik

% Renaming untuk dipakai tocloft
\addto\captionsenglish{
    \renewcommand{\contentsname}{DAFTAR ISI}
    %\renewcommand{\abstractname}{\huge Abstrak}
    \renewcommand{\refname}{Referensi}
    \renewcommand{\listfigurename}{DAFTAR GAMBAR}
    \renewcommand{\listtablename}{DAFTAR TABEL}
    \renewcommand{\lstlistlistingname}{DAFTAR KODE}
    \renewcommand{\figurename}{Gambar}
    \renewcommand{\tablename}{Tabel}
    \renewcommand{\lstlistingname}{Kode}
    \renewcommand{\bibname}{DAFTAR PUSTAKA}
}

% Textbox Code
\lstset{
    basicstyle=\ttfamily\small,
    frame=single,
    breaklines=true,
    tabsize=2,
    numbers=left,
    numberstyle=\tiny,
    keywordstyle=\color{blue!70!black},
    commentstyle=\color{gray},
    stringstyle=\color{red!70!black},
    showstringspaces=false
}

% Folder Assets
\graphicspath{
    {assets/images/}
        {assets/files/}
        {assets/references/}
        {logbook/}
}

% Logbook Table
\newenvironment{logtable}
{
\begin{longtable}{| >{\centering\arraybackslash}m{1cm} | >{\centering\arraybackslash}m{3.5cm} | >{\raggedright\arraybackslash}m{8.1cm} | }\hline
    \textbf{No}           &
    \textbf{Hari/Tanggal} &
    \textbf{Keterangan}                                                        \\
    \hline
    \endfirsthead

    % Header Table untuk Page Selanjutnya
    \hline
    \textbf{No}           &
    \textbf{Hari/Tanggal} &
    \textbf{Keterangan}                                                        \\
    \endhead
}
{
\end{longtable}
}

% For logbook table (log entry) -> insert new row
\newcommand{\logentry}[4]{
#1 & #2 &
\fontsize{11}{14}\selectfont{#3
\vspace{4pt}
#4}
\\ \hline
}

% For logbook table (log list & log detail) -> tujuan & pelaksanaan
\newenvironment{loglist}
{
\textbf{Tujuan:}
\begin{itemize}[noitemsep, topsep=2pt, leftmargin=12pt, label=\textbullet]
}
{
\end{itemize}
}

\newenvironment{logdetail}
{
\textbf{Pelaksanaan:}
\begin{enumerate}[noitemsep, topsep=2pt, leftmargin=15pt]
}
{
\end{enumerate}
}

%=====================================
